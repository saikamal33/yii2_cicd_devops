server {
    listen 80;
    server_name your.domain.com;

    location / {
        proxy_pass http://localhost:8080;  # Adjust to exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

